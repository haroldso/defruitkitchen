<h1>This is area index</h1>

<% @area.each_with_index do | t, index| %>
  <div>
    <%= link_to t do %>
    <%= "#{index+1}. #{t.name}" %>
    <% end %>
  </div>
<% end %>

<div>
  <%= link_to "Create new area!", new_area_path %>
</div>